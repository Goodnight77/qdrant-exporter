# config for qdrant exporter

global:
  scrape_interval: 2s  # how often to scrape
  scrape_timeout: 10s  # timeout for each scrape

scrape_configs:
  - job_name: 'qdrant-exporter'
    static_configs:
      - targets: ['localhost:9999']  #  exporter @
