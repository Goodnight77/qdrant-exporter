# config for qdrant exporter

global:
  scrape_interval: 5s  # how often to scrape
  scrape_timeout: 3s  # timeout for each scrape

scrape_configs:
  - job_name: 'qdrant-exporter'
    static_configs:
      - targets: ['localhost:9999']  #  exporter @
